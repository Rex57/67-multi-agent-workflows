# Claude Code Bootstrap

Use `AGENTS.md` as the canonical instruction file for this repository.

After reading it:

1. Read the first section of `.agents/docs/PROJECT_MEMORY.md`.
2. Read `.agents/README.md`.
3. Follow `AGENTS.md` and keep shared behavior in `AGENTS.md` and `.agents/*`; only place Claude-specific notes here when a shared update would be wrong.
