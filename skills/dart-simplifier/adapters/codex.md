# Codex Adapter: Dart Code Simplifier

Read `../SKILL.md`, then simplify the target Dart or Flutter files while preserving behavior. Follow `.agents/docs/*` when present, report touched files, and include validation status.
